* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Arial Rounded MT Bold", "Arial", sans-serif;
}

body {
  background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460);
  min-height: 100vh;
  overflow-x: hidden;
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20px;
  position: relative;
}

.star {
  position: absolute;
  background-color: white;
  border-radius: 50%;
  animation: twinkle 3s infinite;
}

@keyframes twinkle {
  0%,
  100% {
    opacity: 0.3;
  }
  50% {
    opacity: 1;
  }
}

.container {
  max-width: 1100px;
  width: 100%;
  margin: 0 auto;
  position: relative;
  z-index: 10;
}

header {
  text-align: center;
  padding: 30px 20px;
  position: relative;
}

h1 {
  font-size: 4.5rem;
  color: #ffde59;
  text-shadow:
    0 0 20px #ff9a00,
    0 0 40px #ff9a00;
  margin-bottom: 10px;
  letter-spacing: 3px;
  background: linear-gradient(to right, #ff5e7d, #ff9a00, #ffde59);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.birthday-girl {
  font-size: 3.2rem;
  color: #fff;
  background: linear-gradient(90deg, #ff5e7d, #ff9a00);
  padding: 15px 40px;
  border-radius: 60px;
  display: inline-block;
  margin-top: 10px;
  box-shadow: 0 10px 30px rgba(255, 94, 125, 0.5);
  transform: rotate(-2deg);
  border: 5px solid #ffde59;
  position: relative;
  overflow: hidden;
}

.birthday-girl:before {
  content: "";
  position: absolute;
  top: -10px;
  left: -10px;
  right: -10px;
  bottom: -10px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.2),
    transparent
  );
  transform: translateX(-100%);
}

.birthday-girl:hover:before {
  animation: shine 1.5s ease;
}

@keyframes shine {
  100% {
    transform: translateX(100%);
  }
}

.hero-section {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 40px;
  padding: 40px 20px;
  margin: 30px 0;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
  border-radius: 30px;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.cake-container {
  flex: 1;
  min-width: 350px;
  text-align: center;
  position: relative;
  padding: 20px;
  background: rgba(0, 0, 0, 0.2);
  border-radius: 20px;
}

.cake {
  width: 350px;
  height: 250px;
  margin: 0 auto 30px;
  position: relative;
  transition: transform 0.5s;
}

.cake:hover {
  transform: translateY(-10px);
}

.cake-bottom {
  width: 280px;
  height: 100px;
  background: linear-gradient(to bottom, #f8c8dc, #e75480);
  border-radius: 20px;
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -15px 0 #d32f75,
    0 10px 20px rgba(0, 0, 0, 0.3);
  transition: transform 1.5s ease;
}

.cake-middle {
  width: 230px;
  height: 85px;
  background: linear-gradient(to bottom, #ffafbd, #e75480);
  border-radius: 20px;
  position: absolute;
  bottom: 90px;
  left: 50%;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -12px 0 #d32f75,
    0 8px 15px rgba(0, 0, 0, 0.2);
  transition: transform 1.5s ease;
}

.cake-top {
  width: 180px;
  height: 70px;
  background: linear-gradient(to bottom, #ff9eb5, #e75480);
  border-radius: 20px;
  position: absolute;
  bottom: 165px;
  left: 50%;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -10px 0 #d32f75,
    0 6px 12px rgba(0, 0, 0, 0.2);
  transition: transform 1.5s ease;
}

.candle {
  width: 24px;
  height: 70px;
  background: linear-gradient(to bottom, #4cd964, #2ecc71);
  position: absolute;
  bottom: 235px;
  left: 50%;
  transform: translateX(-50%);
  border-radius: 12px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
  z-index: 2;
}

.flame {
  width: 30px;
  height: 50px;
  background: radial-gradient(#ffde59, #ff9a00, transparent 70%);
  border-radius: 50% 50% 20% 20%;
  position: absolute;
  bottom: 305px;
  left: 50%;
  transform: translateX(-50%);
  opacity: 0;
  z-index: 3;
  box-shadow: 0 0 30px #ff9a00;
  transition: opacity 0.5s;
}

.flame.lit {
  opacity: 1;
  animation: flicker 0.8s infinite alternate;
}

@keyframes flicker {
  0%,
  100% {
    transform: translateX(-50%) scale(1, 1);
    box-shadow: 0 0 30px #ff9a00;
  }
  50% {
    transform: translateX(-50%) scale(1.1, 0.9);
    box-shadow:
      0 0 40px #ff9a00,
      0 0 60px #ff5e7d;
  }
}

.message-box {
  flex: 1;
  min-width: 350px;
  padding: 30px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(15px);
  border-radius: 25px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.message-box h2 {
  color: #ffde59;
  margin-bottom: 20px;
  font-size: 2.2rem;
  text-shadow: 0 0 10px rgba(255, 222, 89, 0.5);
}

.message-box p {
  line-height: 1.7;
  font-size: 1.3rem;
  margin-bottom: 20px;
  color: rgba(255, 255, 255, 0.9);
}

.birthday-wish {
  font-style: italic;
  font-weight: bold;
  color: #ffde59;
  font-size: 1.4rem;
  text-align: center;
  padding: 15px;
  background: rgba(255, 222, 89, 0.1);
  border-radius: 15px;
  margin-top: 20px;
  border-left: 5px solid #ffde59;
}

.interactive-buttons {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  margin-top: 30px;
}

.action-button {
  background: linear-gradient(90deg, #ff5e7d, #ff9a00);
  color: white;
  border: none;
  padding: 18px 35px;
  font-size: 1.4rem;
  border-radius: 50px;
  cursor: pointer;
  transition: all 0.3s;
  box-shadow: 0 8px 20px rgba(255, 94, 125, 0.4);
  display: flex;
  align-items: center;
  gap: 15px;
  position: relative;
  overflow: hidden;
}

.action-button:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 25px rgba(255, 94, 125, 0.6);
}

.action-button:active {
  transform: translateY(0);
}

.action-button:after {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.2),
    transparent
  );
  transform: translateX(-100%);
}

.action-button:hover:after {
  animation: shine 1.5s ease;
}

.action-button i {
  font-size: 1.6rem;
}

.knife-button {
  background: linear-gradient(90deg, #5ac8fa, #007aff);
  display: none;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.5s, transform 0.5s;
}

/* Cake cutting line animation */
.cut-cake-animation {
  position: absolute;
  width: 10px;
  height: 250px;
  background: linear-gradient(
    to right,
    transparent,
    rgba(255, 255, 255, 0.8),
    transparent
  );
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  z-index: 4;
  opacity: 0;
  pointer-events: none;
}

.cake-cut {
  animation: cutEffect 2s forwards;
}

@keyframes cutEffect {
  0% {
    opacity: 1;
    transform: translateX(-50%) scaleY(0);
  }
  50% {
    opacity: 1;
    transform: translateX(-50%) scaleY(1);
  }
  100% {
    opacity: 0;
    transform: translateX(-50%) scaleY(1);
  }
}

/* Cake split effect - FIXED */
/* .cake-split .cake-bottom {
  transform: translateX(-50%) translateX(-40px) !important;
  box-shadow:
    inset 0 -15px 0 #d32f75,
    0 10px 20px rgba(0, 0, 0, 0.3),
    -5px 0 10px rgba(0, 0, 0, 0.2);
} */

/* .cake-split .cake-middle {
  transform: translateX(-50%) translateX(-30px) !important;
  box-shadow:
    inset 0 -12px 0 #d32f75,
    0 8px 15px rgba(0, 0, 0, 0.2),
    -4px 0 8px rgba(0, 0, 0, 0.15);
} */

/* .cake-split .cake-top {
  transform: translateX(-50%) translateX(-20px) !important;
  box-shadow:
    inset 0 -10px 0 #d32f75,
    0 6px 12px rgba(0, 0, 0, 0.2),
    -3px 0 6px rgba(0, 0, 0, 0.1);
} */

/* Create right halves of the cake */
/* .cake-bottom::after,
.cake-middle::after,
.cake-top::after {
  content: '';
  position: absolute;
  width: 100%;
  height: 100%;
  background: inherit;
  border-radius: inherit;
  box-shadow: inherit;
  opacity: 0;
  transition: opacity 1.5s ease;
} */

/* .cake-split .cake-bottom::after {
  opacity: 1;
  left: 40px;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -15px 0 #d32f75,
    0 10px 20px rgba(0, 0, 0, 0.3),
    5px 0 10px rgba(0, 0, 0, 0.2);
} */

/* .cake-split .cake-middle::after {
  opacity: 1;
  left: 30px;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -12px 0 #d32f75,
    0 8px 15px rgba(0, 0, 0, 0.2),
    4px 0 8px rgba(0, 0, 0, 0.15);
} */

/* .cake-split .cake-top::after {
  opacity: 1;
  left: 20px;
  transform: translateX(-50%);
  box-shadow:
    inset 0 -10px 0 #d32f75,
    0 6px 12px rgba(0, 0, 0, 0.2),
    3px 0 6px rgba(0, 0, 0, 0.1);
} */

.features {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 30px;
  padding: 40px 20px;
}

.feature {
  background: rgba(255, 255, 255, 0.08);
  padding: 30px;
  border-radius: 25px;
  width: 320px;
  text-align: center;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
  transition: all 0.3s;
  border: 1px solid rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
}

.feature:hover {
  transform: translateY(-15px);
  background: rgba(255, 255, 255, 0.12);
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
}

.feature i {
  font-size: 3.5rem;
  background: linear-gradient(90deg, #ff5e7d, #ff9a00);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 20px;
}

.feature h3 {
  color: #ffde59;
  margin-bottom: 15px;
  font-size: 1.8rem;
}

.feature p {
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.6;
}

.balloons {
  display: flex;
  justify-content: center;
  gap: 50px;
  margin: 40px 0;
  flex-wrap: wrap;
}

.balloon {
  width: 90px;
  height: 110px;
  border-radius: 50%;
  position: relative;
  animation: float 6s infinite ease-in-out;
  box-shadow: inset -10px -10px 10px rgba(0, 0, 0, 0.2);
  cursor: pointer;
}

.balloon:after {
  content: "";
  position: absolute;
  bottom: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  height: 60px;
  background-color: rgba(255, 255, 255, 0.5);
}

.balloon:nth-child(1) {
  background: radial-gradient(circle at 30% 30%, #ff5e7d, #d32f75);
  animation-delay: 0s;
}

.balloon:nth-child(2) {
  background: radial-gradient(circle at 30% 30%, #4cd964, #2ecc71);
  animation-delay: 0.7s;
}

.balloon:nth-child(3) {
  background: radial-gradient(circle at 30% 30%, #5ac8fa, #007aff);
  animation-delay: 1.4s;
}

.balloon:nth-child(4) {
  background: radial-gradient(circle at 30% 30%, #ffde59, #ff9a00);
  animation-delay: 2.1s;
}

.balloon:nth-child(5) {
  background: radial-gradient(circle at 30% 30%, #af52de, #8e44ad);
  animation-delay: 2.8s;
}

@keyframes float {
  0%,
  100% {
    transform: translateY(0px) rotate(0deg);
  }
  25% {
    transform: translateY(-40px) rotate(5deg);
  }
  75% {
    transform: translateY(-20px) rotate(-5deg);
  }
}

footer {
  text-align: center;
  padding: 40px;
  margin-top: 30px;
  color: rgba(255, 255, 255, 0.7);
  font-size: 1.2rem;
  width: 100%;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 30px 30px 0 0;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.confetti {
  position: fixed;
  width: 15px;
  height: 15px;
  border-radius: 50%;
  top: -10px;
  z-index: 1;
}

/* Smaller Music Player */
.music-player {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(15px);
  border-radius: 40px;
  padding: 10px 15px;
  display: flex;
  align-items: center;
  gap: 10px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.2);
  z-index: 1000;
  width: auto;
  min-width: auto;
}

.music-control {
  background: transparent;
  border: none;
  color: #ffde59;
  font-size: 1.4rem;
  cursor: pointer;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s;
}

.music-control:hover {
  background: rgba(255, 222, 89, 0.1);
  transform: scale(1.1);
}

.volume-slider {
  width: 80px;
  height: 6px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 5px;
  outline: none;
}

.volume-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #ffde59;
  cursor: pointer;
  box-shadow: 0 0 8px rgba(255, 222, 89, 0.5);
}

.song-title {
  font-size: 0.9rem;
  color: #ffde59;
  margin-left: 5px;
  max-width: 120px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Floating Background Balloons - FIXED */
.floating-balloon {
  position: fixed;
  width: 60px;
  height: 80px;
  border-radius: 50%;
  cursor: pointer;
  animation: floating 15s infinite ease-in-out;
  transition: transform 0.3s;
  box-shadow: inset -5px -5px 5px rgba(0, 0, 0, 0.2);
  z-index: 500;
  pointer-events: auto;
}

.floating-balloon:before {
  content: "";
  position: absolute;
  bottom: -15px;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  height: 30px;
  background-color: rgba(255, 255, 255, 0.3);
}

.floating-balloon:hover {
  transform: scale(1.1);
}

.balloon-popped {
  animation: pop 0.5s forwards !important;
  pointer-events: none;
}

@keyframes pop {
  0% {
    transform: scale(1);
    opacity: 1;
  }
  50% {
    transform: scale(1.5);
    opacity: 0.7;
  }
  100% {
    transform: scale(0);
    opacity: 0;
    visibility: hidden;
  }
}

@keyframes floating {
  0%,
  100% {
    transform: translateY(0) translateX(0);
  }
  25% {
    transform: translateY(-30px) translateX(15px);
  }
  50% {
    transform: translateY(-60px) translateX(-10px);
  }
  75% {
    transform: translateY(-30px) translateX(10px);
  }
}

/* Balloons container */
#floatingBalloons {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 500;
}

#floatingBalloons .floating-balloon {
  pointer-events: auto;
}

/* Add balloon pop animation for header balloons */
.balloon.pop {
  animation: pop 0.5s forwards;
}

@media (max-width: 768px) {
  h1 {
    font-size: 3rem;
  }

  .birthday-girl {
    font-size: 2.2rem;
    padding: 12px 25px;
  }

  .cake {
    width: 280px;
  }

  .message-box {
    padding: 20px;
  }

  .music-player {
    bottom: 15px;
    right: 15px;
    left: auto;
    padding: 8px 12px;
  }

  .song-title {
    display: none;
  }

  .floating-balloon {
    width: 50px;
    height: 70px;
  }
}
